import 'package:flutter/material.dart';

const SizedBox kHeight20 = SizedBox(
  height: 20,
);
const SizedBox kHeight10 = SizedBox(
  height: 10,
);

BorderRadius kRadiu20 = BorderRadius.circular(20);
BorderRadius kRadiu10 = BorderRadius.circular(10);

BorderSide side =
    const BorderSide(width: 2, color: Color.fromARGB(163, 0, 0, 0));
